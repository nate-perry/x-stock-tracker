X_BEARER_TOKEN=        # X API v2 bearer token (reads posts)
TWELVE_DATA_API_KEY=   # Twelve Data hourly candles (twelvedata.com)
XAI_API_KEY=           # xAI / Grok summary (api.x.ai)
KV_REST_API_URL=       # Vercel KV REST URL (auto-set by Vercel)
KV_REST_API_TOKEN=     # Vercel KV REST token (auto-set by Vercel)
CRON_SECRET=           # Random secret to protect /api/warm
